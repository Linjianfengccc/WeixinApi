<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace = "com.example.weixin.Services.ChatMsgRepository">
    <insert id="pushNewMsg">
        insert into 消息记录 values (#{from},#{to},#{content},#{date},#{accepted})
    </insert>

</mapper>